export const metadata = {
    title: "KBPa Transportation | KBPayuk",
    description: "Let's check the schedule"
  }
  
  export default function TransportationLayout({ children }) {
    return <>{children}</>
  }